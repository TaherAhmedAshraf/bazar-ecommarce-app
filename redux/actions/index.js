export const SET_USER = (user) => {
    return {
        type: "SET_USER",
        payload: user
    }
}

export const SET_PRODUCTS = (products) => {
    return {
        type: "SET_PRODUCTS",
        payload: products,
    }
}